let ID_BLOCK_BREAK_ENABLED = false;

export const GET_ID_BLOCK_BREAK_ENABLED = () => {
  return GET_ID_BLOCK_BREAK_ENABLED;
};

export const SET_ID_BLOCK_BREAK_ENABLED = (value: boolean) => {
  ID_BLOCK_BREAK_ENABLED = value;
};
